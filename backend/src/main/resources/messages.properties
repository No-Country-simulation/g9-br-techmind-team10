NotBlank={0} is required
Email=Must be a well-formed email address
NotEmpty={0} must not be empty
Size={0} must be between {2} and {1} characters long

# --- ContentRequestDTO(title, text) ---
contentRequestDTO.title=Content title
contentRequestDTO.text=Content text

# --- CorrectionTagsRequestDTO(tags: List<@NotBlank String>) ---
correctionTagsRequestDTO.tags=Tag

# --- LoginRequest(email, password) ---
loginRequest.email=Email
loginRequest.password=Password

# --- RegisterRequest(name, email, password) ---
registerRequest.name=Name
registerRequest.email=Email
registerRequest.password=Password

# --- UserContentTagRequestDTO(name) ---
userContentTagRequestDTO.name=Tag
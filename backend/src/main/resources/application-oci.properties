# Oracle Autonomous Database 26ai on OCI
spring.datasource.url=jdbc:oracle:thin:@${ORACLE_TNS_ALIAS:techmind_tp}?TNS_ADMIN=${ORACLE_WALLET_PATH:C:/ORACLE_DB/TECHMIND}
spring.datasource.username=${ORACLE_DB_USERNAME:TECHMIND_APP}
spring.datasource.password=${ORACLE_DB_PASSWORD}

# OCI should not run demo seed that deletes/reinserts data
spring.flyway.locations=classpath:db/migration
# Oracle Database 26ai Free running locally in Docker
spring.datasource.url=${ORACLE_DATASOURCE_URL:jdbc:oracle:thin:@//localhost:1521/FREEPDB1}
spring.datasource.username=${ORACLE_DB_USERNAME:TECHMIND_APP}
spring.datasource.password=${ORACLE_DB_PASSWORD:TechmindApp123}

# Local environment receives demo/test data
spring.flyway.locations=classpath:db/migration,classpath:db/testdata

# Level trust DS Model
content.trust.low-threshold=0.6

# Exceptions
spring.mvc.log-resolved-exception=true
spring.mvc.throw-exception-if-no-handler-found=true
spring.resources.add-mappings=false

#Security JWT
jwt.secret=${JWT_SECRET:rU3umP5KXMyS0fdqfB496MDoxJ0kHphf}

# Model Prediction
model.service.base-url=${MODEL_SERVICE_URL:http://localhost:8081}

# URI
problem.base-uri=${PROBLEM_BASE_URI:http://localhost:8080}
